# Domain: Git

Canonical skills for cross-project Git/GitHub workflow tasks (PR flow,
concurrent task coordination, etc.). Empty in v1 — candidates identified in
the v1 plan (`ush-github-task-flow`, `ush-concurrent-edit-coordination`) have
not been migrated yet.
