{
  "version": 2,
  "status": "PASS R — REAL ENVIRONMENT. Interprets deployment reachability / real environment behavior, not pure Hub routing quality (see docs/DESIGN.md). Positive cases are modelVisible=false: a live Codex session (codex exec) followed by a quota-free `codex debug prompt-input` diagnostic CONFIRMED all six Hub skills are structurally unreachable in this machine's real, unmodified environment — crowded out of the model-visible skill list by ~330 unrelated third-party skills sharing the same user-level skill root (~/.agents/skills). This is a REACHABILITY FAILURE, not a semantic false negative, and is excluded from precision/recall by scripts/routing-eval.mjs. Negative cases R1/R2 completed live 2026-09-06 (codex exec -s read-only --json, real account, no bypass) and are CONFIRMED true negatives (zero tool calls occurred in either session). A third live case (R3), an explicit-name reachability probe for ush-repo-evidence-plan, was retried live 2026-09-08 (codex exec -s read-only --json, real account, quota available) after the earlier 2026-09-06 attempt was blocked mid-turn by usage-limit exhaustion — it completed this time and is CONFIRMED reachable via explicit naming despite the same modelVisible=false crowding-out: the session's own event stream shows a command_execution reading the exact installed ~/.agents/skills/ush-repo-evidence-plan/SKILL.md via Get-Content (not native skill-loading — the model located and read the file itself after being told the skill's name), followed by an agent_message narrating \"I'm applying `ush-repo-evidence-plan`\" and a final response matching the skill's own documented Result Contract fields (status/summary/proposedChanges/verification/risks) and Safety Contract (remained read-only; git status --porcelain confirmed zero file mutations). This is explicit invocation, not implicit routing, and — same as the Cursor evidence below — is NOT part of any precision/recall computation.",
  "platform": "codex",
  "environment": "real (unmodified user Codex environment: all installed marketplaces, all third-party skills present)",
  "cases": [
    {
      "id": "phase-1.3-real-implicit-1-repo-evidence-plan",
      "task": "Inspect the repository evidence and give me a plan showing which files/functions should change, without editing anything.",
      "expectedSkillIds": ["ush-repo-evidence-plan"],
      "actualSkillIds": null,
      "modelVisible": false,
      "notes": "Confirmed unreachable via `codex debug prompt-input` (2026-09-06): zero ush-* entries in the 337-entry model-visible skill list, truncated alphabetically before 'u'."
    },
    {
      "id": "phase-1.3-real-implicit-2-game-meeting-plan",
      "task": "Turn these meeting notes into a scoped game implementation plan with locks, dependencies, and open questions.",
      "expectedSkillIds": ["ush-game-meeting-plan"],
      "actualSkillIds": null,
      "modelVisible": false,
      "notes": "Same reachability evidence as phase-1.3-real-implicit-1."
    },
    {
      "id": "phase-1.3-real-implicit-3-concurrent-edit-coordination",
      "task": "Check whether two independent tasks are likely to edit overlapping files based on the real git diff.",
      "expectedSkillIds": ["ush-concurrent-edit-coordination"],
      "actualSkillIds": null,
      "modelVisible": false,
      "notes": "Same reachability evidence. Also note: even if reachable, the expected candidate applies only when real diff-inspection evidence is available; otherwise the deterministic eligibility filter should BLOCK (MISSING_TOOL) before semantic routing even matters."
    },
    {
      "id": "phase-1.3-real-negative-1-semver-explanation",
      "task": "Explain what semantic versioning means.",
      "expectedSkillIds": [],
      "actualSkillIds": [],
      "notes": "CONFIRMED true negative — live codex exec session (2026-09-06, -s read-only --json, real account) completed with zero tool-call events of any kind; the model answered directly with no Hub skill involvement. No filesystem/git mutation."
    },
    {
      "id": "phase-1.3-real-negative-2-summarize-paragraph",
      "task": "Summarize this paragraph: The quarterly report showed steady growth in the northern region, while the southern branch experienced a slight decline due to supply chain delays. Management plans to address this by diversifying suppliers next quarter.",
      "expectedSkillIds": [],
      "actualSkillIds": [],
      "notes": "CONFIRMED true negative — live codex exec session (2026-09-06, -s read-only --json, real account) completed with zero tool-call events of any kind. No filesystem/git mutation."
    }
  ]
}
