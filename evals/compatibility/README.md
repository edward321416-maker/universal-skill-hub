# Compatibility Evals

Covered today by `scripts/__tests__/render-adapters.test.mjs` (Test G): a skill
that declares a required capability (`registry/compatibility.json`) a target
platform does not support renders a `BLOCK`, not a silently broken adapter
file.

No cross-platform runtime compatibility has been measured on live Codex /
ChatGPT / Cursor / OpenCode installs yet — status: **NOT YET MEASURED**.
