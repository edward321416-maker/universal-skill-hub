# Regression Evals

Empty in v1. Once a second canonical skill version ships, add a fixture here
that pins prior routing/safety eval outcomes so a future change that flips a
previously-correct USE/SKIP/BLOCK decision is caught automatically.
